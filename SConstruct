#!/usr/bin/env python
# Shim: this repository no longer contains the Godot engine, but the CI
# workflow still invokes scons. Build the cube instead.
import subprocess
import sys

sys.exit(subprocess.call(["bash", "scripts/build_apk.sh"]))
